# Generated by package.sh - staging copy of the submission payload.
# Never edit files here; edit plugin/ and re-run ./package.sh.
# The leading slash anchors this to the project root: an unanchored "build/"
# would also swallow plugin/amd/build/, which Moodle serves in production mode.
/build/

# Cookie jars written by the test harness (deleted on exit, ignored in case of a crash).
/tests/.tmp/

# The submission archive is rebuilt from source by ./package.sh.
# Force-add it only if you want the exact submitted artefact in history:
#   git add -f CSI3140_Lab5_Submission.zip
CSI3140_Lab5_Submission.zip
